<style>
    .svc-section-label {
        color: #94a3b8;
        font-size: .72rem;
        font-weight: 700;
        letter-spacing: .08em;
        margin-bottom: .65rem;
        text-transform: uppercase;
    }

    .svc-grid {
        display: grid;
        gap: .75rem;
    }

    .svc-grid-general,
    .svc-grid-family {
        grid-template-columns: repeat(4, minmax(0, 1fr));
    }

    .svc-tile {
        align-items: flex-start;
        background: #fff;
        border: 1.5px solid #e2e8f0;
        border-radius: 10px;
        cursor: pointer;
        display: flex;
        flex-direction: column;
        justify-content: center;
        min-height: 80px;
        padding: .75rem 1rem;
        transition: border-color .18s, background .18s, box-shadow .18s;
    }

    button.svc-tile {
        font: inherit;
        text-align: left;
        width: 100%;
    }

    .svc-tile:hover { border-color: #7FB8A0; background: #F6FBF8; }
    .svc-tile.selected { border-color: #2F8F6B; background: #EAF7F1; box-shadow: 0 0 0 3px rgba(47,143,107,.15); }
    .svc-tile:focus-visible { outline: 3px solid rgba(47,143,107,.3); outline-offset: 2px; }
    .svc-tile-wide { grid-column: span 2; }
    .svc-name { color: #1a1a1a; font-size: .875rem; font-weight: 700; line-height: 1.3; }
    .svc-sub { color: #ea580c; font-size: .72rem; font-style: italic; font-weight: 600; margin-top: 2px; }
    .svc-price { color: #b45309; font-size: .82rem; font-weight: 700; margin-top: 5px; }
    .svc-free { color: #15803d; }

    @@media (max-width: 767.98px) {
        .svc-grid-general, .svc-grid-family { grid-template-columns: repeat(2, minmax(0, 1fr)); }
    }
</style>
